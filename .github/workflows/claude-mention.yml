name: Claude Code

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  issues:
    types: [opened, assigned]
  pull_request_review:
    types: [submitted]

jobs:
  claude:
    uses: swayloop/.github/.github/workflows/claude-mention.yml@main
    secrets: inherit
    permissions:
      contents: read
      pull-requests: read
      issues: read
      id-token: write
      actions: read
