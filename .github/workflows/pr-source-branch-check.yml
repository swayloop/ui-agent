name: pr-source-branch-check

# main 으로의 PR 이 dev 외 브랜치에서 오는 것을 차단.
# swayloop/.github 의 reusable workflow 호출.

on:
  pull_request:
    branches: [main]

jobs:
  pr-source-branch-check:
    uses: swayloop/.github/.github/workflows/pr-source-branch-check.yml@main
